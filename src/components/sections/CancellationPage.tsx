import { Layout } from "../Layout";
import { Cancelacion } from "./cancelacion";

const CancellationPage = () => {
    return (
        <Layout title="iAmigo - Cancelación de autorización">
            <Cancelacion />
        </Layout>
    );
};

export default CancellationPage;
